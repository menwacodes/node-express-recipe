<!DOCTYPE html>
<html>
  <head>
    <title>Recipes</title>
  </head>
  <body>

    <h1>Recipes</h1>
    <ul>
      <% for (let recipe of recipes) { %>
          <li><%= recipe.name %></li>
      <% } %>
    </ul>

  </body>
</html>
